# GLIE: Generative Late-Interaction Embeddings
# Python 3.10+
#
# Install torch first, matched to your CUDA version, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# ColQwen2 v1.0 needs transformers<=4.49 for its adapter to load; use a separate
# environment for that sweep if your default is newer.

torch>=2.1.0
colpali-engine>=0.3.4
transformers>=4.45.0
peft>=0.13.0
datasets>=2.19.0
accelerate>=0.30.0

numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
pillow>=10.0.0
